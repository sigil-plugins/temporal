# Stable release manifest; publication remains an independently reviewed gate.
schema_version = 4
name = "temporal"
version = "0.1.0"
description = "Bounded Temporal WorkflowService client"
license = "MIT"

[component]
file = "target/component/temporal.wasm"
entrypoint = "sigil:temporal/client@0.1.0"

[requires]
sigil = ">=0.35.0, <0.36.0"
host_api = "=1.3.0"

[capabilities]
grpc-unary = true

[repository]
source = "github:sigil-plugins/temporal"
